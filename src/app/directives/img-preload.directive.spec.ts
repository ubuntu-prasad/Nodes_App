import { ImgPreloadDirective } from './img-preload.directive';

describe('ImgPreloadDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgPreloadDirective();
    expect(directive).toBeTruthy();
  });
});
